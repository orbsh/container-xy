packages:
  nushell:
    repo: nushell/nushell
    uri: https://github.com/nushell/nushell/releases/download/{version}/nu-{version}-{arch}-unknown-linux-musl.tar.gz
    version:
    - trim
    unpack:
    - strip 1
    - filter nu nu_plugin_query
    - wrap bin
  pueue:
    repo: Nukesor/pueue
    uri:
    - https://github.com/Nukesor/pueue/releases/download/{version}/pueue-{arch}-unknown-linux-musl
    - https://github.com/Nukesor/pueue/releases/download/{version}/pueued-{arch}-unknown-linux-musl
    unpack:
    - mv pueue-{arch}-unknown-linux-musl pueue
    - mv pueued-{arch}-unknown-linux-musl pueued
    - chmodx pueue pueued
    - wrap bin
  websocat:
    repo: vi/websocat
    uri: https://github.com/vi/websocat/releases/download/{version}/websocat.{arch}-unknown-linux-musl
    unpack:
    - mv websocat.{arch}-unknown-linux-musl websocat
    - chmodx websocat
    - wrap bin
  ferron:
    repo: ferronweb/ferron
    uri: https://github.com/ferronweb/ferron/releases/download/{version}/ferron-{version}-{arch}-unknown-linux-musl.zip
    unpack:
    - filter ferron ferron-passwd ferron-yaml2kdl ferron-precompress
    - wrap bin
